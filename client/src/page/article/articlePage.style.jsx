import styled from "@emotion/styled";

export const ArticlePageDiv = styled('div')({
    // border : '1px solid red',
    display: 'flex',
    flexDirection: 'column',
    padding: '2em 2em',
});

export const ArticleSection = styled('div')({
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    flexWrap: 'wrap',
    // flexDirection: 'column',
});