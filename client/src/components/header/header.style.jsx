import AppBar from "@mui/material/AppBar";
import { styled } from "@mui/system";

export const ModifiedAppBar = styled(AppBar)({
    backgroundImage: 'linear-gradient(to bottom right, #3d0ba9, #5a19a9);',
    backgroundColor: 'transparent',
    color: 'white',
    position: 'sticky',
    top: '0',
});
